# managerws_root:
#     path: '/{url}'
#     defaults: { _controller: ManagerWebService:WebService:default, url: '' }
#     requirements:
#         url: '.*'

managerwebservice_root:
    path: '/{other}'
    defaults: { _controller: ManagerWebServiceBundle:WebService:default, other: '' }
    requirements:
        other: '.*'
