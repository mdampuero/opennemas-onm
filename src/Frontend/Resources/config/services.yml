services:
    frontend_login_success_handler:
        class:     Frontend\EventListener\LoginSuccessHandler
        arguments: [ '@security.token_storage', '@instance', '@router', '@session', '@google_recaptcha', '@application.log' ]
    frontend.listener.share_by_email:
        class:     Frontend\EventListener\ShareByEmailListener
        arguments: [ '@service_container' ]
        tags:
            - { name: kernel.event_listener, event: kernel.request, method: onKernelRequest, priority: 98 }
