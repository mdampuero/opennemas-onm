services:
    frontend_login_success_handler:
        class:     Frontend\EventListener\LoginSuccessHandler
        arguments: [ '@security.token_storage', '@router', '@google_recaptcha', '@application.log' ]
