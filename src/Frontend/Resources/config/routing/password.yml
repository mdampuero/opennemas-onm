frontend_password_change:
    path:     /password/change
    defaults: { _controller  : FrontendBundle:Password:change}

frontend_password_request:
    path:     /password/request
    defaults: { _controller  : FrontendBundle:Password:request}

frontend_password_reset:
    path:     /password/reset
    defaults: { _controller  : FrontendBundle:Password:reset}

frontend_password_update:
    path:     /password/update
    defaults: { _controller  : FrontendBundle:Password:update}
