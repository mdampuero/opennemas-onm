frontend_books:
    path: /libros/
    defaults: { _controller: FrontendBundle:Books:frontpage }
    options:
        l10n: true

frontend_book_show_with_slug:
    path: /libro/{category_slug}/{slug}/{id}.{_format}
    defaults: { _controller: FrontendBundle:Books:show, _format: html }
    requirements:
        category_slug: '[^/]+'
        slug         : '([^/]+)?'
        id           : '([0-9]+)'
    options:
        l10n: true
