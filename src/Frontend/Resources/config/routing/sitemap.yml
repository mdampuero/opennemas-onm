frontend_sitemap:
    path: /sitemap{action}.{format}
    defaults: { _controller: FrontendBundle:Sitemap:index, format: 'xml' }
    requirements:
        format: 'xml|xml.gz'
        action: '|image|news|tag|video|web'
