frontend_static_page:
    path: /estaticas/{slug}.{format}
    defaults: { _controller: FrontendBundle:StaticPage:show, format:html }
    requirements:
        format : html|htm
        slug    : '[^/.]+'
    options:
        l10n: true
