backend_password_reset:
    path:     /password/reset
    defaults: { _controller  : BackendBundle:Password:reset}

backend_password_request:
    path:     /password/request
    defaults: { _controller  : BackendBundle:Password:request}

backend_password_change:
    path:     /password/change
    defaults: { _controller  : BackendBundle:Password:change}

backend_password_update:
    path:     /password/update
    defaults: { _controller  : BackendBundle:Password:update}
