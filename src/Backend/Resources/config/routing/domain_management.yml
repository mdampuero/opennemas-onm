admin_domain_management:
    path: /domain-management
    defaults: { _controller  : BackendBundle:DomainManagement:list }
    options:
        expose: true

admin_domain_management_add:
    path: /domain-management/add
    defaults: { _controller  : BackendBundle:DomainManagement:add }
    options:
        expose: true

admin_domain_management_show:
    path: /domain-management/show
    defaults: { _controller  : BackendBundle:DomainManagement:show }
    options:
        expose: true
