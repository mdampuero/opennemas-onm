backend_domains_list:
    path: /domains
    defaults: { _controller: BackendBundle:Domain:list }
    options:
        expose: true

backend_domain_add:
    path: /domains/add
    defaults: { _controller: BackendBundle:Domain:add }
    options:
        expose: true
