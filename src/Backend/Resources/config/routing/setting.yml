backend_settings_general:
    path:     /settings/general/
    defaults: { _controller: BackendBundle:GeneralSettings:config }

backend_settings_appearance:
    path:     /settings/appearance/
    defaults: { _controller: BackendBundle:AppearanceSettings:config }

backend_settings_language:
    path:     /settings/language/
    defaults: { _controller: BackendBundle:LanguageSettings:config }

backend_settings_internal:
    path:     /settings/internal/
    defaults: { _controller: BackendBundle:InternalSettings:config }

backend_settings_external:
    path:     /settings/external/
    defaults: { _controller: BackendBundle:ExternalSettings:config }

backend_settings_sitemap:
    path:     /settings/sitemap/
    defaults: { _controller: BackendBundle:SitemapSettings:config }

backend_settings_master:
    path:     /settings/master/
    defaults: { _controller: BackendBundle:MasterSettings:config }

backend_settings_theme:
    path:     /settings/theme
    defaults: { _controller: BackendBundle:ThemeSettings:config }
