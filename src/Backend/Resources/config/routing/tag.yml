backend_tags_list:
    path:     /tags
    defaults: { _controller  : BackendBundle:Tag:list }
    options:
        expose: true

backend_tags_config:
    path:     /tags/config
    defaults: { _controller  : BackendBundle:Tag:config }
    options:
        expose: true
