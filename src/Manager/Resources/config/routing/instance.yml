manager_instance_create:
    path: /instances/new
    options:
        expose: true

manager_instances_list:
    path: /instances
    options:
        expose: true

manager_instance_show:
    path: /instances/{id}
    requirements:
        id: \d+
    options:
        expose: true
