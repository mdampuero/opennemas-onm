manager_instances_list:
    path: /instances
    options:
        expose: true

manager_instance_show:
    path: /instance/{id}
    requirements:
        id: \d+
    options:
        expose: true

manager_instance_create:
    path: /instance/create
    options:
        expose: true
