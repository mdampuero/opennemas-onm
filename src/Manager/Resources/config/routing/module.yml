manager_module_create:
    path: /modules/create
    options:
        expose: true

manager_module_show:
    path: /modules/{id}
    requirements:
        id: \d+
    options:
        expose: true

manager_modules_list:
    path: /modules
    options:
        expose: true
