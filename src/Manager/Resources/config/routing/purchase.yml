manager_purchases_list:
    path: /purchases
    options:
        expose: true

manager_purchase_show:
    path: /purchases/{id}/show
    requirements:
        id: \d+
    options:
        expose: true

manager_purchase_create:
    path: /purchases/create
    options:
        expose: true
