entity:
    name: UserGroup
    properties:
        id:         integer
        name:       string
        privileges: array
    mapping:
        table: user_group
        converters:
            default:
                class:     Common\ORM\Database\Data\Converter\BaseConverter
                arguments: [ '@orm.metadata.user_group' ]
        repositories:
            instance:
                class:     Common\ORM\Database\Repository\BaseRepository
                arguments: [ '@cache', '@orm.connection.instance', '@orm.metadata.user_group' ]
            manager:
                class:     Common\ORM\Database\Repository\BaseRepository
                arguments: [ '@cache', '@orm.connection.manager', '@orm.metadata.user_group' ]
        persisters:
            instance:
                class:     Common\ORM\Database\Persister\BasePersister
                arguments: [ '@cache', '@orm.connection.instance', '@orm.metadata.user_group' ]
            manager:
                class:     Common\ORM\Database\Persister\BasePersister
                arguments: [ '@cache', '@orm.connection.manager', '@orm.metadata.user_group' ]
        columns:
            id:
                type: integer
                options: { default: null, unsigned: true, autoincrement: true }
            name:
                type: string
                options: { default: null, notnull: false, length: 50 }
            privileges:
                type: text
                options: { default: null, length: 65532, notnull: false }
        index:
            -
                name: PRIMARY
                columns: [ id ]
                primary: true
                unique: true
                flags: {  }
