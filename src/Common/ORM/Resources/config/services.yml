parameters:
    orm.config_path: %kernel.root_dir%/config/orm

services:
    orm.loader:
        class: Common\ORM\Core\Loader\Loader
        arguments: [ %orm.config_path%, %kernel.environment% ]

    orm.manager:
        class: Common\ORM\Core\EntityManager
        arguments: [ @service_container ]
