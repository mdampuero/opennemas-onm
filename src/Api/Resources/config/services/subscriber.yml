services:
    core.subscriber.category:
        class:     Api\EventSubscriber\CategorySubscriber
        arguments: [ '@core.instance', '@core.helper.template_cache', '@core.helper.varnish' ]
        tags:
            - { name: kernel.event_subscriber }
