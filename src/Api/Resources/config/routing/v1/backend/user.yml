backend_ws_users_settings_list:
    path:     /users/settings
    defaults: { _controller  : ApiBundle:V1/Backend/User:listSettings }
    methods:  [ GET ]
    options:
        expose: true

backend_ws_users_settings_save:
    path:     /users/settings
    defaults: { _controller  : ApiBundle:V1/Backend/User:saveSettings }
    methods:  [ PUT ]
    options:
        expose: true
