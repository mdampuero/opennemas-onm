backend_ws_newsletter_list:
    path:     /newsletters
    defaults: { _controller: BackendWebServiceBundle:Newsletters:list }
    options:
        expose: true

backend_ws_newsletter_delete:
    path:     /newsletter/{id}/delete
    defaults: { _controller  : BackendWebServiceBundle:Newsletters:delete }
    options:
        expose: true
