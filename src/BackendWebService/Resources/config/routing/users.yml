backend_ws_user_delete:
    path:     /users/{id}/delete
    defaults: { _controller: BackendWebServiceBundle:Users:delete }
    methods:  [ GET ]
    options:
        expose: true

backend_ws_user_patch:
    path:     /users/{id}
    defaults: { _controller: BackendWebServiceBundle:Users:patch }
    methods:   [ PATCH ]
    options:
        expose: true

backend_ws_users_delete:
    path:     /users
    defaults: { _controller: BackendWebServiceBundle:Users:deleteSelected }
    methods:  [ DELETE ]
    options:
        expose: true

backend_ws_users_export:
    path:     /users.csv
    defaults: { _controller: BackendWebServiceBundle:Users:export }
    methods:  [ GET ]
    options:
        expose: true

backend_ws_users_list:
    path:     /users
    defaults: { _controller: BackendWebServiceBundle:Users:list }
    methods:  [ GET ]
    options:
        expose: true

backend_ws_users_patch:
    path:     /users
    defaults: { _controller: BackendWebServiceBundle:Users:patchSelected }
    methods:  [ PATCH ]
    options:
        expose: true
