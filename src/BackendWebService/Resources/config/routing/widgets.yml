backend_ws_widgets_list:
    path:     /widgets
    defaults: { _controller: BackendWebServiceBundle:Widgets:list }
    methods:  [ GET ]
    options:
        expose: true

backend_ws_widget_get_form:
    path:     /widgets/{uuid}/form
    defaults: { _controller: BackendWebServiceBundle:Widgets:getForm }
    options:
        expose: true
