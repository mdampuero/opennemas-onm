backend_ws_media_picker_mode:
    path:     /picker
    defaults: { _controller: BackendWebServiceBundle:MediaPicker:picker }
    options:
        expose: true

backend_ws_media_picker_get_months:
    path:     /picker/months
    defaults: { _controller: BackendWebServiceBundle:MediaPicker:getMonths }
    options:
        expose: true
