backend_ws_keywords_list:
    path:     /keywords
    defaults: { _controller: BackendWebServiceBundle:Keywords:list }
    options:
        expose: true

backend_ws_pclave_remove_permanently:
    path:     /pclaves/{id}/remove
    defaults: { _controller: BackendWebServiceBundle:Keywords:removePermanently }
    options:
        expose: true
