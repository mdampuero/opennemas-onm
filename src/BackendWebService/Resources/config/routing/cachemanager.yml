backend_ws_cachemanager_list:
    path:     /cachemanager
    defaults: { _controller: BackendWebServiceBundle:CacheManager:list }
    options:
        expose: true

backend_ws_cachemanager_remove:
    path:     /cachemanager/remove
    defaults: { _controller: BackendWebServiceBundle:CacheManager:remove }
    options:
        expose: true
