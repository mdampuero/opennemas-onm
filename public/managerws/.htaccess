<IfModule mod_rewrite.c>
    RewriteEngine On

    RewriteBase /managerws/

    RewriteCond $1 !\.(php)$ [NC]
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^(.*)$ app.php [QSA,L]
</IfModule>
