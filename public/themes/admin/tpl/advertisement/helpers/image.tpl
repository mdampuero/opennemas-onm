<html>
  <head>
    <style>
      body {
        margin: 0;
        overflow: hidden;
        padding: 0;
      }
    </style>
  </head>
  <body>
    <a target="_blank" href="{{$url}}" rel="nofollow">
      <img alt="{{$category}}" src="{{$url}}" width="{{$width}}" height="{{$height}}"/>
    </a>
  </body>
</html>
