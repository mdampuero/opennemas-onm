<html>
  <head>
    <style>
      body {
        display: table;
        margin: 0;
        overflow: hidden;
        padding: 0;
        text-align: center;
      }

      img {
        height: auto;
        max-width: 100%;
      }
    </style>
  </head>
  <body>
    <div class="content">
      <a target="_blank" href="{{$url}}" rel="nofollow">
        <img alt="{{$category}}" src="{{$src}}" width="{{$width}}" height="{{$height}}"/>
      </a>
    </div>
  </body>
</html>
