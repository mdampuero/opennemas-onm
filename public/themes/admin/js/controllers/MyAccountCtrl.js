angular.module('BackendApp.controllers')
  /**
   * Handle actions for article inner.
  */
  .controller('MyAccountCtrl', ['$rootScope', '$scope',
  function($rootScope, $scope) {
    'use strict';

    console.log('test');
  }
]);
